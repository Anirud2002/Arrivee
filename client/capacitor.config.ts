import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.locationreminder',
  appName: 'client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
